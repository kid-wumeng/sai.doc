module.exports =

   name: 'len'
   desc: '如果想让每一个页面的切换都有淡出 (fade) 效果，我们需要创建一个所有路由共用的 CSS 文件。所以我们可以在 assets/ 目录下创建这个文件：'

   signs: [
      require('./MongoDB.Collection(name, options)')
      require('./len(string, mode)')
      require('./PI')
   ]